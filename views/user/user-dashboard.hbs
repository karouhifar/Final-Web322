<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Dashboard_user</title>
    <link rel="stylesheet" href="../data/CSS4/style4.css" />
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/4.7.0/css/font-awesome.min.css" />
    {{> style}}
</head>

<body>
    {{>header userInfo}}
    <main>
        <h1>Hello {{userInfo.Fname}}</h1>
        <div class="show">

        </div>
        <table class="Table">
            <caption>{{#green}}Data using Handlebar.JS{{/green}}</caption>
            <thead>
                <tr>
                    <th>Index ID</th>
                    <th>Type User</th>
                    <th>First Name</th>
                    <th>Last Name</th>
                    <th>E-mail</th>
                    <th>Password</th>
                </tr>
            </thead>
            <tbody>
                {{#if userInfo}}
                <tr>
                    <td>{{userInfo.id}}</td>
                    <td>{{userInfo.type}}</td>
                    <td>{{userInfo.Fname}}</td>
                    <td>{{userInfo.Lname}}</td>
                    <td>{{userInfo.email}}</td>
                    {{#if userInfo.privacy}}
                    <td>Private data</td>
                    {{else}}
                    <td>{{userInfo.password}}</td>

                    {{/if}}
                </tr>
                {{else}}
                <tr>
                    <td colspan="5">No Data Available</td>
                </tr>
                {{/if}}

            </tbody>
        </table>
    </main>
    {{>footer}}
</body>

</html>